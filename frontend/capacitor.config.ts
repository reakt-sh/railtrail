import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'sh.reakt.railtrail',
  appName: 'RailTrail',
  webDir: 'dist/frontend/browser'
};

export default config;
